# Step 1: Initialize git
git init

# Step 2: Add files to staging area
git add app.py

# Step 3: Commit with a message
git commit -m "Initial commit: basic user registration form"

# Step 4: Add GitHub repository (replace with your actual repo URL)
git remote add origin https://github.com/<your-username>/flask-registration.git

# Step 5: Push to main branch
git push -u origin main


# Step 6: Create a new branch
git branch update-form

# Step 7: Switch to the new branch
git checkout update-form

# Step 8: Add and commit the updated code
git add app.py
git commit -m "Added Department field to registration form"

# Step 9: Switch back to main branch
git checkout main

# Step 10: Merge the changes from update-form
git merge update-form

# Step 11: Push final merged version to GitHub
git push origin main
